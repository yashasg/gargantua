---
# gargantua-6xce
title: 'Engine selection: Template vs MLX with Settings toggle'
status: completed
type: task
priority: normal
created_at: 2026-04-20T14:06:09Z
updated_at: 2026-04-21T01:13:36Z
parent: gargantua-8igf
blocked_by:
    - gargantua-mgqr
---

`LocalAIService` takes an injected `AIInferenceEngine` but there's no way
for the app to actually select one. Wire the Template ↔ MLX choice into
Settings and pick a sensible default based on availability.

## Scope

- Add a Settings-level preference (`UserDefaults`-backed via the existing
  settings surface) for preferred engine: `.template` | `.mlx`.
- Change the `LocalAIService` construction site to honor the preference
  and fall back to `TemplateInferenceEngine` when MLX is selected but
  the model isn't downloaded or MLX isn't available for any reason.
- Settings UI: a simple picker / toggle. When MLX is picked but the
  model isn't yet on disk, surface the existing model-download affordance
  rather than silently doing nothing.

## Out of scope

- Actual MLX implementation (tracked under `gargantua-ddaa`).
- Changing the `AIExplanation.source` labeling; `.ai` vs `.rule` is
  already correct.

## Acceptance

- [x] Settings exposes the engine preference and persists it
- [x] App startup picks the right engine; missing model → fallback to
      Template and `AIExplanation.source == .rule` remains correct on
      the fallback path
- [x] Tests cover: MLX selected + model present, MLX selected + no model,
      Template selected


## Completed

**Files changed:**
- Sources/GargantuaCore/Services/AIEnginePreference.swift (new)
- Sources/GargantuaCore/Services/LocalAIService.swift
- Sources/GargantuaCore/Services/TemplateInferenceEngine.swift
- Sources/GargantuaCore/Views/SettingsView.swift
- Sources/Gargantua/MainContentView.swift
- Tests/GargantuaCoreTests/Services/LocalAIServiceTests.swift
- Tests/GargantuaCoreTests/Services/AIAdvisoryControllerTests.swift

**Key decisions:**
- UserDefaults-backed engine preference (`preferredAIEngine`) keeps startup selection available before SwiftData is ready.
- `AIInferenceEngineFactory` resolves Template vs MLX from preference + model state; MLX is selected only when the downloaded model path validates as an HF/MLX directory.
- `LocalAIService` can swap engines in place so controllers keep their references, and load-time failures now fall back to YAML/rule output for advisory explanation paths.
- `TemplateInferenceEngine.load` accepts model directories as ready state so Template preference works with the current directory-staged model manager.
- Settings uses the existing AI Model card with a segmented Engine picker and shows the existing download controls when MLX is selected without a ready model.

**Verification:**
- `swift test --filter LocalAIService`: 46 tests passed
- `swift test`: 830 tests passed
- `swift build -c debug -Xswiftc -warnings-as-errors`: passed

---
*Auto-generated by PASIV*

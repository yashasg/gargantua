{
  "key": "secrets",
  "score": 100,
  "weight": 1.5,
  "skill_version": "1.0.0",
  "applicable": true,
  "notes": "No real secrets detected. Gitleaks flagged 1 string as a generic-secret-assignment, but it is a clear test placeholder ('gtua_test_token_12345678901234567890' — sequential digits) used as a `private static let validToken` in MCPSSETransportTests.swift. Per spec, known test/example values are not flagged. .env files are properly gitignored and none are committed. Git history (453 commits) scanned — no history-only secrets found.",
  "metrics": {
    "files_scanned": 442,
    "git_tracked_files": 442,
    "toolchain_signals": ["gitleaks", "semgrep", "trivy"],
    "confidence_breakdown": {
      "deterministic": 1,
      "heuristic": 0
    },
    "category_breakdown": {
      "test_placeholder": 1,
      "real_secrets": 0,
      "env_files_committed": 0,
      "weak_random": 0,
      "history_only_secrets": 0
    },
    "git_history_scanned": true,
    "commit_count": 453,
    "gitignore_env_check": "pass"
  },
  "findings": [
    {
      "id": "secrets.test-token-placeholder",
      "title": "Gitleaks flagged a test fixture token",
      "severity": "info",
      "confidence": "deterministic",
      "category": "test_fixture",
      "file": "Tests/GargantuaCoreTests/Services/MCP/MCPSSETransportTests.swift",
      "line": 130,
      "rule_id": "generic-secret-assignment",
      "description": "Gitleaks detected a long opaque value assigned to a 'Token' variable. The value 'gtua_****' is a clear test placeholder (a `private static let validToken` constant in a test file) using sequential-digit padding, not a real secret. Treated as info-only per the 'never flag known test/example values' rule. If desired, suppress this in .gitleaks.toml via an [allowlist] regex on the literal value.",
      "snippet": "private static let validToken = \"gtua_****REDACTED****\"",
      "fix_prompt": "# Suppress: gitleaks false positive on test fixture token\n\n## Context\nGitleaks flagged a test placeholder used by the MCP SSE transport bearer-auth tests as a generic secret. The token is a hardcoded test constant (`gtua_test_token_12345678901234567890`) — clearly synthetic, not a real credential. The test relies on the literal value to verify the bearer-auth handshake. Removing or changing it would break the test for no security benefit.\n\n## Location\n- File: Tests/GargantuaCoreTests/Services/MCP/MCPSSETransportTests.swift\n- Line: 130\n- Dimension: secrets / info\n\n## Required change\nAdd an allowlist entry to `.gitleaks.toml` so future scans no longer flag this fixture:\n\n```toml\n[allowlist]\ndescription = \"MCP SSE transport test fixtures\"\npaths = [\n  '''Tests/GargantuaCoreTests/Services/MCP/MCPSSETransportTests\\.swift''',\n]\nregexes = [\n  '''gtua_test_token_\\d+''',\n]\n```\n\n## Constraints\n- Do not change the test fixture value — tests assert on this exact token.\n- Scope the allowlist as narrowly as possible (path + regex), not a global ignore.\n\n## Verification\n- Run: `gitleaks detect --source=. --report-format=json --report-path=- --config=.gitleaks.toml` — should report 0 leaks.\n- Run: `swift test --filter MCPSSETransportTests` — bearer-auth tests still pass."
    }
  ]
}

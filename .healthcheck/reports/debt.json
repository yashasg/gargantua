{
  "key": "debt",
  "score": 100,
  "weight": 1.1,
  "skill_version": "1.0.0",
  "applicable": true,
  "notes": "Exceptional technical debt hygiene. Zero TODO/FIXME/HACK/XXX markers across all 291 Swift source files, shell scripts, YAML, and markdown. Zero commented-out code blocks. Zero deprecated API usage. Zero stub implementations in production code. No long-lived feature flags. 447 `// MARK: -` section markers demonstrate active organizational discipline. The `unsafeFlags` linker workaround in Package.swift (for embedding the scheduler's Info.plist as a Mach-O section) is 0 days old and well-documented — well below the 90-day debt threshold. `#if os(macOS)` conditionals in ScheduledScanService and LaunchAtLoginService are correct platform-guard idioms, not conditional debt. `#if DEBUG` wraps a `#Preview` — idiomatic SwiftUI.",
  "metrics": {
    "total_markers": 0,
    "aged_markers_90d": 0,
    "aged_markers_365d": 0,
    "commented_out_blocks": 0,
    "commented_out_lines": 0,
    "deprecated_api_usages": 0,
    "stub_implementations": 0,
    "stale_feature_flags": 0,
    "median_marker_age_days": 0,
    "oldest_marker_age_days": 0,
    "mark_section_markers": 447,
    "files_scanned": 291,
    "git_blame_calls": 1
  },
  "findings": [
    {
      "id": "debt.unsafe-flags-spm",
      "title": "unsafeFlags linker workaround in Package.swift (0 days old — informational)",
      "severity": "info",
      "confidence": "deterministic",
      "file": "Package.swift",
      "line": 49,
      "description": "The GargantuaScheduler target uses `unsafeFlags` to pass raw linker arguments (-Xlinker -sectcreate) to embed the Info.plist as a Mach-O __TEXT/__info_plist section. This is a known SPM workaround for macOS helper tools. It is 0 days old and documented in the package header. Not debt yet — flagged for awareness so it can be migrated to a first-class SPM feature when one becomes available.",
      "snippet": ".unsafeFlags([\"-Xlinker\", \"-sectcreate\", \"-Xlinker\", \"__TEXT\", \"-Xlinker\", \"__info_plist\", ...])",
      "fix_prompt": "# Note: unsafeFlags linker workaround in Package.swift\n\n## Context\nSPM does not yet support embedding arbitrary Mach-O sections natively. The `-sectcreate __TEXT __info_plist <path>` linker flag is needed to embed the scheduler's Info.plist so launchd can inspect it. This is a well-established workaround. Track it so it can be migrated if SPM gains native support.\n\n## Location\n- File: Package.swift\n- Line: 49\n- Dimension: debt / info\n\n## Required change\nNo change needed now. When Swift Package Manager adds a first-class way to embed Info.plists (e.g., via `resources` with a type that handles embedding), migrate to that API and remove the `unsafeFlags` block.\n\nMonitor: https://forums.swift.org/t/property-list-embedding-support-for-spm/\n\n## Constraints\n- Do NOT remove the unsafeFlags block without a working alternative — the scheduler won't be recognized by launchd without its embedded plist.\n\n## Verification\n- Run: `swift build -c release --target GargantuaScheduler`\n- Run: `otool -s __TEXT __info_plist .build/release/GargantuaScheduler` — should print the embedded plist XML."
    }
  ]
}
